package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Primary Dental Teal & Mint Palette (Soft, Professional, Clean)
val TealPrimary = Color(0xFF00796B)         // Deep soft medical teal
val TealPrimaryDark = Color(0xFF004D40)     // Dark teal for text on mint
val TealLight = Color(0xFF48A999)           // Medium teal
val MintSoft = Color(0xFFE0F2F1)            // Soft pastel mint container
val MintUltraLight = Color(0xFFF0FDF4)      // Soft pastel green tint
val MintAccent = Color(0xFF26A69A)          // Interactive accent

// Soft Blue Accents
val SkySoft = Color(0xFFE1F5FE)             // Soft pastel blue
val SkyDeep = Color(0xFF0277BD)             // Medical ocean blue
val SkyAccent = Color(0xFF0288D1)           // Active blue

// Cream & Clean Backgrounds
val CreamSurface = Color(0xFFFBFDFA)        // Warm clean porcelain
val BackgroundLight = Color(0xFFF6FAF9)     // Soft light mint-grey
val SurfaceCard = Color(0xFFFFFFFF)         // Pure white card
val SurfaceCardAlt = Color(0xFFF1F8F6)      // Very soft mint card

// Alert & Information Accents (Soft, never garish)
val SoftAmber = Color(0xFFFFF8E1)           // Soft warning badge (plaque/tartar)
val AmberText = Color(0xFFB45309)
val SoftRose = Color(0xFFFFEBEE)            // Soft alert badge (cavity/sariawan)
val RoseText = Color(0xFFBE123C)
val SoftGreen = Color(0xFFDCFCE7)           // Success badge
val GreenText = Color(0xFF15803D)

// High-contrast readable typography colors
val TextPrimary = Color(0xFF0F2624)         // Dark slate teal (very readable)
val TextSecondary = Color(0xFF475569)       // Muted slate
val TextTertiary = Color(0xFF64748B)        // Light slate
val BorderSoft = Color(0xFFE2E8F0)          // Soft clean borders
